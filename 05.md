touch main.tf
# lambdaを使用するためのS3バケットを作成する。
# lambda関数と外部からのリクエストを受け付けるURLの生成。

# シェルスクリプト作成（シェルスクリプトとは、シェルを介して実行されるコマンドをまとめたテキストファイルである）
touch lambda_deploy.sh


ロールを生成のところを修正したらInternal Server Error% が治った
Cloudwatch Logsへの書き込みを付与し