やったこと

touch ec2.tf
# OSはubuntu/images/hvm-ssd/ubuntu-jammy-22.04-amd64-server-*でオーナーをCanonicalにした。awsinstanceの設定はt3.microにした。

terraform fmt
(terraformのよく使うコマンド
・fmt  コードを綺麗に整える
・init  初期化
・plan  実行の確認
・apply  実行
)

# 以前設定した.terraformがあったので削除
rm -rf .terraform
# 初期化
terraform init
terraform plan
terraform apply
# インスタンスの作成を確認

# ssh接続をして操作したい
# portが閉じていることをnmapというコマンドで確認（IPやホストにどんなサービス・ポートが開いてるか調べるコマンド）
touch vpc.tf
# vpc.tfにセキュリティグループの設定を記入。
# VPC_IDの情報、セキュリティーグループの作成宣言、アウトバンドルール作成宣言、インバウンドルール作成宣言、IPアドレスからのアクセスを設定、プロトコルの許可設定、開くポートの設定など。
# terraform.tfvarsにVPC_IDを設定し、gitignoreにterraform.tfvarsを入れる。

# セキュリティグループの設定が終わったらec2.tfに戻り、IDを貼り付け:aws_security_group.ssh_enable.id



terraform init
terraform plan
terraform apply

# 次にSSHの鍵を設定。
# ec2.tfに記載

cat ~/.ssh/id_ed25519.pub
# EC2のコマンドラインに接続
ssh ubuntu@ec2-13-113-220-170.ap-northeast-1.compute.amazonaws.com
# 入れたので抜ける
exit
# ec2.tfの"aws_instance"部分をコメントアウトし
terraform init
terraform plan
terraform apply
# インスタンスの停止を確認
