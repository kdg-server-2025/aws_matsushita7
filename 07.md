# variables.tfとdatabase.tfを作ってDBの設定をする。
# 後者にはAWS RDSのインスタンスを作成するコードを書く

# ネットワークの情報も必要と言われたのでnetwork.tfに追記
# 一旦作成
terraform apply
# RDSは直接インターネットを介して接続できないので、ec2を踏み台サーバーにして接続していく。


# SSM接続をするためのやつ
brew install --cask session-manager-plugin

# privatesubnetのIDをtfvarsに書いてあげる
A
subnet-0c10d7e432bcca096
C
subnet-022cc2e91fe704086

# 路線変更。IAM 認証を使用したプロキシへの接続を試す。上記の踏み台を使うやり方のコードは削除。
# どうやらその機能も本来よく使う目的とは少し逸れているので、直接接続できるようにセキュリティの甘いやり方でやる。
# サブネットはRDSの存在する場所（ネットワークゾーン）を決める物で、セキュリティグループはネットワークを通すかどうかなどの基本的な項目。今回は簡単にやるのでサブネットの設定はしない。

# rds_endpointを調べるコマンド
terraform output rds_endpoint
# （rds_endpointはどこに入れとく？？）

# とりあえず先生のを参考にvpc.tfにセキュリティグループの設定。
# PostgreSQL クライアントをインストール。psqlコマンドが使える
brew install postgresql

psql -h macha-db.clos4008gdti.ap-northeast-1.rds.amazonaws.com -U macha -d postgres -p 5432

# エラー対応。ここからのIPアドレスからしか受け付けないcidr_ipv4の設定がバンタンのIPだったので、自宅に変更。自宅のIPを調べるコマンドは以下。
curl ifconfig.me<!-- こっちはIPv6の方 -->
curl -4 ifconfig.me<!-- こっちはIPv4の方 -->

# 自宅のIPアドレスに変更。もう一度applyを行う。
terraform plan
terraform apply
# 接続をする。
psql -h macha-db.clos4008gdti.ap-northeast-1.rds.amazonaws.com -U macha -d postgres -p 5432
# テーブル作成
CREATE TABLE test_table (
id SERIAL PRIMARY KEY,
name TEXT
);
# カラム追加
INSERT INTO test_table (name) VALUES ('hello');
# ２回目も試してみる
INSERT INTO test_table (name) VALUES ('goodbye');
# 閲覧
select * from test_table;
# 退出
exit

# インスタンスの削除。aws_db_instanceをコメントアウト。
terraform apply


# 先生の話：コミットはこまめに行っていい。なぜなら自分がした変更内容を管理しやすかったり、見返したときにすぐに理解できたりするから。planしてエラーを直すたびにしても良い。コミットメッセージはわかりやすく書く。tfstateの内容は機密にしたいのでgitignoreに入れる、今回はできなかったからS3に保存。いらないコードはコメントアウトで残さず、適宜消した方がいい、必要になったらまた作ればよい。自動で作られるファイルは整合性が取れずエラーが起きたりするから消してまた再生成すると治る（やや非推奨）。ファイルは消さない方が良い→あまりわかっていない、discripsion（説明）にはわかりやすい説明を。main.tfにはAWSの基本設定があるので、03のブランチからマージして取り込み、活用。基本は03から派生させた方が良い。

# 対策→mdファイルにはファイルごとの目的とやったことを記す。コミットメッセージにはこまめな変更内容を記載。コミットメッセージの決まり、バグ修正はfix:、新規機能追加はadd:、削除はremove:、変更取り消しはrevert:、機能修正はupdate:、仕様変更はchange:、
# エラー文は翻訳につっこんで絶対理解する。機能を一時的に無効にするdisable:


